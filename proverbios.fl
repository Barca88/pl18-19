
%option noyywrap
%{
  #include <stdio.h>
  #include <stdlib.h>

  FILE* out;

  char* title;
  char* quote;
%}

%x PROV TEXT AUTOR

%% 

\<title>Provérbios[^<]+ { BEGIN
                          fprintf(out,"<h1>%s</h1>\n",yytext+7);
                          title = strdup(yytext+7);
                        }

%%

int main(int argc, char** argv){
    
    if(argc > 1){
    	yyin = fopen(argv[1], "r");
      out = fopen(argv[2],"w");
    }

    yylex();

    return 0;
}
